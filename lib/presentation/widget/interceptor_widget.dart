import 'package:flutter/widgets.dart';

class InterceptorWidget extends StatelessWidget {
  final Widget child;
  const InterceptorWidget({super.key, required this.child});
  @override
  Widget build(BuildContext context) => child;
} 